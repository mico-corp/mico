get_filename_component(mico_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

if(NOT TARGET mico::mico-python)
    include("${mico_CMAKE_DIR}/mico-python-targets.cmake")
endif()